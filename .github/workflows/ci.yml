name: Commit Phase

on:
  push:
    branches: ['main']
  pull_request:
    branches: ['main']

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo test --all-features

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - name: Lint check
        run: cargo clippy -- -D clippy::pedantic -W clippy::nursery

  build_and_upload:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable

      - name: Build release
        run: cargo build --release

      - name: List Files
        run: ls -la

      - name: List Files
        run: ls -la target/release

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: companion_connect
          path: target/release/companion_connect

      - name: Upload target
        uses: actions/upload-artifact@v4
        with:
          name: companion_connect
          path: target
